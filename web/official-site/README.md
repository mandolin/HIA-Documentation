# Official Site Baseline

The W-P4.4 baseline uses `docs/published-jsdoc-usage.md` as the first public usage entry before a full documentation site generator is selected.

The current verified public path is:

```bash
pnpm run smoke:published-jsdoc
```

Future docs site work can promote that published JSDoc walkthrough into this directory without changing the smoke command.
